Imports System.Reflection

' The application's own assembly attributes. Everything it shares with Skrzynki.Core - company,
' product, copyright, version - is in SharedAssemblyInfo.vb at the root of the solution.
'
' Keep this file saved as UTF-8 with a byte order mark: the description carries Polish letters,
' and without the mark the compiler reads them in the build machine's ANSI code page.

<Assembly: AssemblyTitle("Boxes: Skrzynki")>
<Assembly: AssemblyDescription("Gra logiczna, w której wcielasz się w magazyniera!")>